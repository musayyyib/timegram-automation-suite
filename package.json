{
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "cypress": "^13.12.0",
    "cypress-mochawesome-reporter": "^3.8.1",
    "cypress-xpath": "^2.0.1"
  },
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:run:all": "cypress run --spec 'cypress/e2e/all_tests_spec.js'",
    "cypress:run:all:headed": "cypress run --spec 'cypress/e2e/all_tests_spec.js' --headed"
  },
  "dependencies": {
    "faker": "^5.5.3"
  }
}
